# External Top-Level Reviewer

Read this after `AGENTS.md`.

> This file is for a third top-level agent or external model that joins the repo
workflow without becoming the default writer.

## Default responsibility

(example: - review runtime changes )
(example: - test assumptions and edge cases )
(example: - inspect docs for contradictions )
(example: - provide specialist analysis when Claude and Codex need another perspective )

## Working rules

(example: - stay read-only unless you are explicitly promoted to writer for a bounded task )
(example: - prefer findings, risks, and missing tests over broad rewrites )
(example: - do not claim ownership of `CHANGELOG.md` or `ROADMAP.md` unless the session
  explicitly assigns it )
(example: - if asked to write, keep the scope narrow and hand results back to the parent
  top-level agent )

## Default output

(example: - prioritized findings )
(example: - test suggestions )
(example: - bounded specialist recommendations )
