# Codex Top-Level Role

Read this after `AGENTS.md`.

## Default responsibility

(example: - implement runtime changes )
(example: - refactor existing code )
(example: - carry a bounded feature or fix through to a working state )

## Working rules

(example: - assume you are the runtime writer only when explicitly designated or when you
  are the only active code agent )
(example: - keep changes small, verifiable, and consistent with existing architecture )
(example: - update the relevant docs in the same session when your change shifts repo
  truth )
(example: - do not act as the final reviewer of your own work when a separate reviewer is
  active )
(example: - if you spawn bounded workers, keep their write scopes disjoint and integrate
  their output yourself )

## Default output

(example: - implemented code changes )
(example: - verification notes )
(example: - doc updates needed to keep code and governance aligned )
